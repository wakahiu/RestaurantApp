<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="restaurantapp.restaurantapp" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".loginactivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".registeractivity"
            android:label="registermode">
        </activity>
        <activity
            android:name=".MenuItemListActivity"
            android:label="@string/title_menuitem_list"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".MenuItemDetailActivity"
            android:label="@string/title_menuitem_detail"
            android:parentActivityName=".MenuItemListActivity"
            android:theme="@style/AppTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="restaurantapp.restaurantapp.MenuItemListActivity" />
        </activity>
        <activity
            android:name=".RestaurantItemListActivity"
            android:label="@string/title_restaurantitem_list"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".RestaurantItemDetailActivity"
            android:label="@string/title_restaurantitem_detail"
            android:parentActivityName=".RestaurantItemListActivity"
            android:theme="@style/AppTheme" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="restaurantapp.restaurantapp.RestaurantItemListActivity" />
        </activity>
    </application>

</manifest>
